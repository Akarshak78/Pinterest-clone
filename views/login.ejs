<!DOCTYPE html>
<html>
<head>
  <title>Log in</title>
  <link rel="stylesheet" href="/stylesheets/register.css"/>
</head>
<body>
  <div class="container">
    <div class="content">
      <!-- Pinterest logo -->
      <img src="https://i.pinimg.com/originals/d3/d1/75/d3d175e560ae133f1ed5cd4ec173751a.png" alt="pin logo" class="img1" />
      <p class="header">Log in to see more</p>

      <!-- ✅ ✅ Flash error messages -->
      <% if (error && error.length > 0) { %>
        <div class="flash-error">
          <% error.forEach(function(msg) { %>
            <p><%= msg %></p>
          <% }); %>
        </div>
      <% } %>

      <!-- ✅ ✅ Flash success messages (optional) -->
      <% if (success && success.length > 0) { %>
        <div class="flash-success">
          <% success.forEach(function(msg) { %>
            <p><%= msg %></p>
          <% }); %>
        </div>
      <% } %>

      <!-- ✅ Login form -->
      <form action="/login" method="POST">
        <input type="text" name="username" placeholder="Username" class="detail" required /><br />
        <input type="password" name="password" placeholder="Password" class="detail" required /><br />
        <button type="submit" class="btn int">Log in</button>
      </form>

      <p class="or">OR</p>

      <!-- ✅ Social buttons (optional) -->
      <a href="#" class="btn fbk">
        <i class="fab fa-facebook fa-lg" style="color: white; padding-right: 10px"></i>
        Continue with Facebook
      </a>
      <br />
      <a href="#" class="btn ggl">
        <i class="fab fa-google" style="color: rgb(11, 241, 22); padding-right: 10px"></i>
        Continue with Google
      </a>

      <footer>
        <p>
          By continuing, you agree to Pinterest's
          <b>Terms of Service, Privacy Policy.</b>
        </p>
        <hr />
        <p>Not on Pinterest yet? <a href="/register">Register</a></p>
      </footer>
    </div>
  </div>
</body>
</html>
